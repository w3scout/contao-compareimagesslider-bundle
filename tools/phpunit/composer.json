{
    "require": {
        "phpunit/phpunit": "^12.4",
        "contao/test-case": "^6.0"
    },
    "minimum-stability": "dev",
    "prefer-stable": true,
    "config": {
        "audit": {
            "abandoned": "ignore"
        }
    }
}
